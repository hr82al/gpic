[package]
name = "gpic"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.104"
chrono = "0.4.45"
clap = { version = "4.6.6", features = ["derive"] }
image = "0.25"
rand = "0.10"
reqwest = { version = "0.13", default-features = false, features = ["blocking", "rustls", "system-proxy"] }

[dev-dependencies]
tempfile = "3.27.0"
