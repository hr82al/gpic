# gpic Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Консольная утилита, которая без аргументов кладёт в текущий каталог свежую сгенерированную картинку под разрешение монитора.

**Architecture:** Шесть независимых модулей (`cli`, `screen`, `prompt`, `provider`, `resize`, `output`), связываемых в `main`. Картинка запрашивается у бесплатного анонимного API `image.pollinations.ai` в целевой пропорции, приходит с фиксированным бюджетом ≈0.59 Мп, затем локально масштабируется Lanczos3 до точных целевых размеров. Внешних бинарников нет — всё в чистом Rust.

**Tech Stack:** Rust edition 2024, clap 4 (derive), reqwest 0.13 (blocking), image 0.25, rand 0.10, chrono 0.4, anyhow 1.

**Spec:** `docs/superpowers/specs/2026-08-28-gpic-design.md`

## Global Constraints

- Rust edition 2024 (в `Cargo.toml` уже стоит), тулчейн ≥ 1.85; на машине 1.97.1.
- Никаких внешних бинарников (`convert`, `ffmpeg`) — только крейты.
- Фичи reqwest 0.13 именно такие: `blocking`, `rustls`, `system-proxy`. В 0.13 фича называется `rustls`, **не** `rustls-tls`; поддержка `HTTP_PROXY`/`HTTPS_PROXY` требует `system-proxy`. Проверено эмпирически.
- Пиксельный бюджет API ≈ 589 824 px; запрошенное соотношение сторон соблюдается, абсолютный размер — нет. Клиент запрашивает целевые W×H и масштабирует ответ сам.
- Ни один юнит-тест не ходит в сеть. Единственный сетевой тест помечен `#[ignore]`.
- Все публичные ошибки — через `anyhow::Result`.
- Сообщения пользователю на русском.

---

### Task 1: Зависимости и разбор аргументов

**Files:**
- Modify: `Cargo.toml`
- Create: `src/cli.rs`
- Modify: `src/main.rs`

**Interfaces:**
- Consumes: ничего.
- Produces: `cli::Args { prompt: Option<String>, resolution: Option<(u32, u32)>, seed: Option<u32>, file: Option<PathBuf> }`, функция `cli::parse_resolution(&str) -> Result<(u32, u32), String>`.

- [ ] **Step 1: Добавить зависимости**

```bash
cd /home/user/mr/ws/gpic
cargo add clap --features derive
cargo add reqwest@0.13 --no-default-features --features blocking,rustls,system-proxy
cargo add image@0.25
cargo add rand@0.10
cargo add chrono
cargo add anyhow
cargo add --dev tempfile
```

- [ ] **Step 2: Написать падающий тест**

Создать `src/cli.rs` целиком из этого блока (тесты есть, реализации нет):

```rust
use clap::Parser;
use std::path::PathBuf;

#[derive(Parser, Debug)]
#[command(name = "gpic", about = "Генератор случайных картинок для рабочего стола")]
pub struct Args {
    /// Промпт; без него будет случайный
    #[arg(short, long)]
    pub prompt: Option<String>,

    /// Разрешение, например 2560x1440; без него — разрешение экрана
    #[arg(short, long, value_parser = parse_resolution)]
    pub resolution: Option<(u32, u32)>,

    /// Seed для воспроизведения конкретной картинки
    #[arg(short, long)]
    pub seed: Option<u32>,

    /// Путь или просто имя файла; без него — gpic-ГГГГММДД-ЧЧММСС.jpg
    pub file: Option<PathBuf>,
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn parses_valid_resolution() {
        assert_eq!(parse_resolution("1920x1200"), Ok((1920, 1200)));
        assert_eq!(parse_resolution("800x600"), Ok((800, 600)));
    }

    #[test]
    fn accepts_uppercase_separator() {
        assert_eq!(parse_resolution("2560X1440"), Ok((2560, 1440)));
    }

    #[test]
    fn rejects_garbage() {
        assert!(parse_resolution("abc").is_err());
        assert!(parse_resolution("1920*1200").is_err());
        assert!(parse_resolution("1920x").is_err());
        assert!(parse_resolution("x1200").is_err());
        assert!(parse_resolution("1920x1200x3").is_err());
    }

    #[test]
    fn rejects_zero_dimensions() {
        assert!(parse_resolution("0x1200").is_err());
        assert!(parse_resolution("1920x0").is_err());
    }

    #[test]
    fn cli_verifies() {
        use clap::CommandFactory;
        Args::command().debug_assert();
    }
}
```

Добавить в `src/main.rs` первой строкой `mod cli;`.

- [ ] **Step 3: Запустить тест и убедиться, что падает**

Run: `cargo test --lib cli 2>&1 | tail -20` (или `cargo test cli`)
Expected: ошибка компиляции — `cannot find function 'parse_resolution' in this scope`.

- [ ] **Step 4: Минимальная реализация**

Вставить в `src/cli.rs` перед блоком `#[cfg(test)]`:

```rust
pub fn parse_resolution(s: &str) -> Result<(u32, u32), String> {
    let lower = s.to_ascii_lowercase();
    let (w, h) = lower
        .split_once('x')
        .ok_or_else(|| format!("ожидался формат ШИРИНАxВЫСОТА, получено «{s}»"))?;

    let parse_side = |v: &str, name: &str| -> Result<u32, String> {
        let n: u32 = v
            .parse()
            .map_err(|_| format!("{name} должна быть числом, получено «{v}»"))?;
        if n == 0 {
            return Err(format!("{name} должна быть больше нуля"));
        }
        Ok(n)
    };

    Ok((parse_side(w, "ширина")?, parse_side(h, "высота")?))
}
```

Замечание: `"1920x1200x3"` отсекается автоматически — `split_once` даст правую часть `"1200x3"`, которая не парсится в `u32`.

- [ ] **Step 5: Запустить тесты**

Run: `cargo test cli`
Expected: PASS, 5 тестов.

- [ ] **Step 6: Коммит**

```bash
git add Cargo.toml Cargo.lock src/cli.rs src/main.rs
git commit -m "feat(cli): разбор аргументов и парсинг разрешения"
```

---

### Task 2: Определение разрешения экрана

**Files:**
- Create: `src/screen.rs`
- Modify: `src/main.rs`

**Interfaces:**
- Consumes: ничего.
- Produces: `screen::detect() -> (u32, u32)`, `screen::FALLBACK: (u32, u32)`.

Внутренние функции `parse_first_mode` и `detect_in` существуют ради тестируемости: `detect_in` принимает корень как параметр, поэтому тест подсовывает временный каталог вместо `/sys/class/drm`.

- [ ] **Step 1: Написать падающие тесты**

Создать `src/screen.rs`:

```rust
use std::path::Path;

pub const FALLBACK: (u32, u32) = (1920, 1200);

#[cfg(test)]
mod tests {
    use super::*;
    use std::fs;

    #[test]
    fn parses_first_mode_line() {
        let modes = "1920x1200\n1920x1080\n1600x1200\n";
        assert_eq!(parse_first_mode(modes), Some((1920, 1200)));
    }

    #[test]
    fn ignores_empty_modes_file() {
        assert_eq!(parse_first_mode(""), None);
        assert_eq!(parse_first_mode("\n"), None);
    }

    #[test]
    fn ignores_unparseable_mode() {
        assert_eq!(parse_first_mode("не режим\n"), None);
    }

    #[test]
    fn picks_connected_output_over_disconnected() {
        let dir = tempfile::tempdir().unwrap();
        let root = dir.path();

        let dead = root.join("card0-DP-1");
        fs::create_dir_all(&dead).unwrap();
        fs::write(dead.join("status"), "disconnected\n").unwrap();
        fs::write(dead.join("modes"), "3840x2160\n").unwrap();

        let live = root.join("card0-HDMI-A-1");
        fs::create_dir_all(&live).unwrap();
        fs::write(live.join("status"), "connected\n").unwrap();
        fs::write(live.join("modes"), "1920x1200\n1280x1024\n").unwrap();

        assert_eq!(detect_in(root), Some((1920, 1200)));
    }

    #[test]
    fn returns_none_when_nothing_connected() {
        let dir = tempfile::tempdir().unwrap();
        let out = dir.path().join("card0-DP-1");
        fs::create_dir_all(&out).unwrap();
        fs::write(out.join("status"), "disconnected\n").unwrap();
        fs::write(out.join("modes"), "1024x768\n").unwrap();

        assert_eq!(detect_in(dir.path()), None);
    }

    #[test]
    fn detect_falls_back_on_missing_root() {
        assert_eq!(
            detect_in(Path::new("/nonexistent-drm-root")).unwrap_or(FALLBACK),
            FALLBACK
        );
    }
}
```

Добавить `mod screen;` в `src/main.rs`.

- [ ] **Step 2: Запустить тесты и убедиться, что падают**

Run: `cargo test screen 2>&1 | tail -20`
Expected: ошибка компиляции — `parse_first_mode` и `detect_in` не найдены.

- [ ] **Step 3: Реализация**

Вставить в `src/screen.rs` после `FALLBACK`:

```rust
/// Первая строка файла `modes` — предпочитаемый режим выхода.
fn parse_first_mode(contents: &str) -> Option<(u32, u32)> {
    let line = contents.lines().next()?.trim();
    let (w, h) = line.split_once('x')?;
    Some((w.parse().ok()?, h.parse().ok()?))
}

/// Ищет первый подключённый выход в дереве вида /sys/class/drm.
fn detect_in(root: &Path) -> Option<(u32, u32)> {
    let mut outputs: Vec<_> = std::fs::read_dir(root)
        .ok()?
        .filter_map(|e| e.ok())
        .map(|e| e.path())
        .collect();
    // Порядок read_dir не определён — сортируем ради воспроизводимости.
    outputs.sort();

    outputs.into_iter().find_map(|dir| {
        let status = std::fs::read_to_string(dir.join("status")).ok()?;
        if status.trim() != "connected" {
            return None;
        }
        let modes = std::fs::read_to_string(dir.join("modes")).ok()?;
        parse_first_mode(&modes)
    })
}

/// Разрешение подключённого монитора; при неудаче — FALLBACK.
///
/// Читаем sysfs, а не xrandr: на Wayland xrandr пуст, а sysfs работает
/// и не требует зависимостей.
pub fn detect() -> (u32, u32) {
    detect_in(Path::new("/sys/class/drm")).unwrap_or(FALLBACK)
}
```

- [ ] **Step 4: Запустить тесты**

Run: `cargo test screen`
Expected: PASS, 6 тестов.

- [ ] **Step 5: Проверить на живой машине**

Добавить временно в `main` вызов `println!("{:?}", screen::detect());`, выполнить `cargo run`, убедиться, что печатается `(1920, 1200)`, затем убрать строку. (Если экран другой — значение должно совпадать с `head -1 /sys/class/drm/card0-HDMI-A-1/modes`.)

- [ ] **Step 6: Коммит**

```bash
git add src/screen.rs src/main.rs
git commit -m "feat(screen): определение разрешения монитора через sysfs"
```

---

### Task 3: Генератор случайных промптов

**Files:**
- Create: `src/prompt.rs`
- Modify: `src/main.rs`

**Interfaces:**
- Consumes: ничего.
- Produces: `prompt::random(rng: &mut impl rand::Rng) -> String`.

Функция принимает RNG параметром, а не создаёт его внутри — иначе её нельзя проверить детерминированно.

- [ ] **Step 1: Написать падающие тесты**

Создать `src/prompt.rs`:

```rust
use rand::prelude::*;

#[cfg(test)]
mod tests {
    use super::*;
    use rand::rngs::StdRng;

    #[test]
    fn is_deterministic_for_a_fixed_seed() {
        let a = random(&mut StdRng::seed_from_u64(7));
        let b = random(&mut StdRng::seed_from_u64(7));
        assert_eq!(a, b);
    }

    #[test]
    fn is_not_empty() {
        let p = random(&mut StdRng::seed_from_u64(1));
        assert!(!p.trim().is_empty());
    }

    #[test]
    fn varies_across_seeds() {
        let variants: std::collections::HashSet<String> =
            (0..50).map(|s| random(&mut StdRng::seed_from_u64(s))).collect();
        assert!(variants.len() > 10, "получено всего {} вариантов", variants.len());
    }

    #[test]
    fn all_lists_are_non_empty() {
        assert!(!SUBJECTS.is_empty());
        assert!(!LIGHT.is_empty());
        assert!(!STYLES.is_empty());
    }
}
```

Добавить `mod prompt;` в `src/main.rs`.

- [ ] **Step 2: Запустить тесты и убедиться, что падают**

Run: `cargo test prompt 2>&1 | tail -20`
Expected: ошибка компиляции — `random`, `SUBJECTS`, `LIGHT`, `STYLES` не найдены.

- [ ] **Step 3: Реализация**

Вставить в `src/prompt.rs` перед блоком тестов:

```rust
const SUBJECTS: &[&str] = &[
    "misty mountain lake",
    "snow-covered pine forest",
    "rocky coastline with breaking waves",
    "rolling hills under a wide sky",
    "quiet northern fjord",
    "desert dunes with long shadows",
    "alpine meadow in bloom",
    "old stone bridge over a river",
    "autumn birch grove",
    "glacier tongue meeting dark water",
    "lavender field stretching to the horizon",
    "storm clouds over open plains",
];

const LIGHT: &[&str] = &[
    "golden hour",
    "blue hour",
    "soft morning fog",
    "dramatic sunset",
    "overcast diffused light",
    "clear starry night",
    "low winter sun",
    "after the rain",
];

const STYLES: &[&str] = &[
    "landscape photography, sharp detail",
    "cinematic wide shot",
    "painterly, rich colors",
    "minimalist composition",
    "high dynamic range photograph",
    "serene and atmospheric",
];

/// Собирает промпт из трёх независимых списков — сотни комбинаций
/// из компактного кода.
pub fn random(rng: &mut impl Rng) -> String {
    let subject = SUBJECTS.choose(rng).expect("SUBJECTS не пуст");
    let light = LIGHT.choose(rng).expect("LIGHT не пуст");
    let style = STYLES.choose(rng).expect("STYLES не пуст");
    format!("{subject}, {light}, {style}, beautiful wallpaper")
}
```

- [ ] **Step 4: Запустить тесты**

Run: `cargo test prompt`
Expected: PASS, 4 теста.

- [ ] **Step 5: Коммит**

```bash
git add src/prompt.rs src/main.rs
git commit -m "feat(prompt): комбинаторный генератор случайных промптов"
```

---

### Task 4: Масштабирование до целевого размера

**Files:**
- Create: `src/resize.rs`
- Modify: `src/main.rs`

**Interfaces:**
- Consumes: ничего.
- Produces: `resize::to(img: image::DynamicImage, w: u32, h: u32) -> image::DynamicImage`.

Модуль называется `resize`, а не `upscale`, потому что при запросе разрешения меньше 0.59 Мп (например `800x600`) картинку нужно уменьшать.

- [ ] **Step 1: Написать падающие тесты**

Создать `src/resize.rs`:

```rust
use image::DynamicImage;
use image::imageops::FilterType;

#[cfg(test)]
mod tests {
    use super::*;

    fn sample(w: u32, h: u32) -> DynamicImage {
        DynamicImage::ImageRgb8(image::RgbImage::new(w, h))
    }

    #[test]
    fn enlarges_to_exact_size() {
        let out = to(sample(971, 607), 1920, 1200);
        assert_eq!((out.width(), out.height()), (1920, 1200));
    }

    #[test]
    fn shrinks_to_exact_size() {
        let out = to(sample(1024, 576), 800, 600);
        assert_eq!((out.width(), out.height()), (800, 600));
    }

    #[test]
    fn keeps_identical_size() {
        let out = to(sample(640, 480), 640, 480);
        assert_eq!((out.width(), out.height()), (640, 480));
    }
}
```

Добавить `mod resize;` в `src/main.rs`.

- [ ] **Step 2: Запустить тесты и убедиться, что падают**

Run: `cargo test resize 2>&1 | tail -20`
Expected: ошибка компиляции — `to` не найдена.

- [ ] **Step 3: Реализация**

Вставить в `src/resize.rs` перед тестами:

```rust
/// Приводит картинку ровно к w×h. Пропорция уже задана запросом к API,
/// поэтому растягивания не происходит и кадрировать нечего.
pub fn to(img: DynamicImage, w: u32, h: u32) -> DynamicImage {
    if img.width() == w && img.height() == h {
        return img;
    }
    img.resize_exact(w, h, FilterType::Lanczos3)
}
```

- [ ] **Step 4: Запустить тесты**

Run: `cargo test resize`
Expected: PASS, 3 теста.

- [ ] **Step 5: Коммит**

```bash
git add src/resize.rs src/main.rs
git commit -m "feat(resize): масштабирование Lanczos3 до точного размера"
```

---

### Task 5: Имя файла и запись

**Files:**
- Create: `src/output.rs`
- Modify: `src/main.rs`

**Interfaces:**
- Consumes: ничего.
- Produces: `output::timestamp() -> String`, `output::resolve_path(arg: Option<PathBuf>, now: &str) -> PathBuf`, `output::save(img: &image::DynamicImage, path: &Path) -> anyhow::Result<()>`.

`resolve_path` принимает готовую строку времени параметром, чтобы тест не зависел от текущих часов.

- [ ] **Step 1: Написать падающие тесты**

Создать `src/output.rs`:

```rust
use anyhow::{Context, Result};
use image::{DynamicImage, ImageFormat};
use std::path::{Path, PathBuf};

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn uses_timestamp_when_no_file_given() {
        let p = resolve_path(None, "20260828-153012");
        assert_eq!(p, PathBuf::from("gpic-20260828-153012.jpg"));
    }

    #[test]
    fn keeps_bare_name_relative_to_cwd() {
        let p = resolve_path(Some(PathBuf::from("wall.png")), "20260828-153012");
        assert_eq!(p, PathBuf::from("wall.png"));
        assert!(p.is_relative());
    }

    #[test]
    fn keeps_explicit_path_untouched() {
        let p = resolve_path(Some(PathBuf::from("/tmp/pics/a.jpg")), "20260828-153012");
        assert_eq!(p, PathBuf::from("/tmp/pics/a.jpg"));
    }

    #[test]
    fn chooses_format_from_extension() {
        assert_eq!(format_for(Path::new("a.png")), ImageFormat::Png);
        assert_eq!(format_for(Path::new("a.jpg")), ImageFormat::Jpeg);
        assert_eq!(format_for(Path::new("a.JPEG")), ImageFormat::Jpeg);
    }

    #[test]
    fn falls_back_to_jpeg_for_unknown_extension() {
        assert_eq!(format_for(Path::new("a.xyz")), ImageFormat::Jpeg);
        assert_eq!(format_for(Path::new("noext")), ImageFormat::Jpeg);
    }

    #[test]
    fn timestamp_has_expected_shape() {
        let t = timestamp();
        assert_eq!(t.len(), 15, "получено «{t}»");
        assert_eq!(t.as_bytes()[8], b'-');
        assert!(t.chars().filter(|c| c.is_ascii_digit()).count() == 14);
    }

    #[test]
    fn writes_a_readable_file() {
        let dir = tempfile::tempdir().unwrap();
        let path = dir.path().join("out.png");
        let img = DynamicImage::ImageRgb8(image::RgbImage::new(8, 6));
        save(&img, &path).unwrap();

        let reread = image::open(&path).unwrap();
        assert_eq!((reread.width(), reread.height()), (8, 6));
    }

    #[test]
    fn reports_missing_directory_clearly() {
        let img = DynamicImage::ImageRgb8(image::RgbImage::new(4, 4));
        let err = save(&img, Path::new("/nonexistent-dir-xyz/out.jpg")).unwrap_err();
        assert!(
            format!("{err:#}").contains("nonexistent-dir-xyz"),
            "ошибка не называет путь: {err:#}"
        );
    }
}
```

Добавить `mod output;` в `src/main.rs`.

- [ ] **Step 2: Запустить тесты и убедиться, что падают**

Run: `cargo test output 2>&1 | tail -20`
Expected: ошибка компиляции — `resolve_path`, `format_for`, `timestamp`, `save` не найдены.

- [ ] **Step 3: Реализация**

Вставить в `src/output.rs` перед тестами:

```rust
/// Метка времени вида 20260828-153012 в локальной зоне.
pub fn timestamp() -> String {
    chrono::Local::now().format("%Y%m%d-%H%M%S").to_string()
}

/// Голое имя остаётся относительным, то есть попадает в текущий каталог.
pub fn resolve_path(arg: Option<PathBuf>, now: &str) -> PathBuf {
    arg.unwrap_or_else(|| PathBuf::from(format!("gpic-{now}.jpg")))
}

/// Формат по расширению; всё незнакомое и безрасширенное — JPEG.
fn format_for(path: &Path) -> ImageFormat {
    path.extension()
        .and_then(|e| e.to_str())
        .and_then(|e| ImageFormat::from_extension(e.to_ascii_lowercase()))
        .unwrap_or(ImageFormat::Jpeg)
}

pub fn save(img: &DynamicImage, path: &Path) -> Result<()> {
    let format = format_for(path);
    // JPEG не умеет альфу — приводим к RGB8, иначе запись падает.
    let to_write = if format == ImageFormat::Jpeg {
        DynamicImage::ImageRgb8(img.to_rgb8())
    } else {
        img.clone()
    };
    to_write
        .save_with_format(path, format)
        .with_context(|| format!("не удалось записать файл {}", path.display()))
}
```

- [ ] **Step 4: Запустить тесты**

Run: `cargo test output`
Expected: PASS, 8 тестов.

- [ ] **Step 5: Коммит**

```bash
git add src/output.rs src/main.rs
git commit -m "feat(output): выбор имени файла и запись картинки"
```

---

### Task 6: Запрос к Pollinations

**Files:**
- Create: `src/provider.rs`
- Modify: `src/main.rs`

**Interfaces:**
- Consumes: ничего.
- Produces: `provider::fetch(prompt: &str, w: u32, h: u32, seed: u32) -> anyhow::Result<image::DynamicImage>`.

`build_url` вынесена отдельно, чтобы проверить сборку URL без сети.

- [ ] **Step 1: Написать падающие тесты**

Создать `src/provider.rs`:

```rust
use anyhow::{Context, Result, bail};
use image::DynamicImage;
use reqwest::Url;
use std::time::Duration;

const BASE: &str = "https://image.pollinations.ai/";
const TIMEOUT: Duration = Duration::from_secs(120);

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn builds_url_with_all_parameters() {
        let url = build_url("mountain lake", 1920, 1200, 42).unwrap();
        assert_eq!(url.path(), "/prompt/mountain%20lake");

        let q: std::collections::HashMap<_, _> = url.query_pairs().into_owned().collect();
        assert_eq!(q["width"], "1920");
        assert_eq!(q["height"], "1200");
        assert_eq!(q["seed"], "42");
        assert_eq!(q["nologo"], "true");
    }

    #[test]
    fn percent_encodes_non_ascii_prompt() {
        let url = build_url("ночной город", 800, 600, 1).unwrap();
        assert!(url.as_str().starts_with("https://image.pollinations.ai/prompt/%D0%BD"));
        assert!(!url.as_str().contains(' '));
    }

    #[test]
    fn encodes_slashes_inside_the_prompt() {
        // Слеш в промпте не должен создавать лишний сегмент пути.
        let url = build_url("a/b", 800, 600, 1).unwrap();
        assert_eq!(url.path(), "/prompt/a%2Fb");
    }

    /// Настоящий сетевой запрос. Запускать вручную:
    /// cargo test -- --ignored provider::tests::fetches_a_real_image
    #[test]
    #[ignore]
    fn fetches_a_real_image() {
        let img = fetch("quiet mountain lake", 1920, 1200, 12345).unwrap();
        assert!(img.width() > 100 && img.height() > 100);
        // Сервис держит пропорцию, а не абсолютный размер.
        let ratio = img.width() as f64 / img.height() as f64;
        assert!((ratio - 1920.0 / 1200.0).abs() < 0.02, "пропорция {ratio}");
    }
}
```

Добавить `mod provider;` в `src/main.rs`.

- [ ] **Step 2: Запустить тесты и убедиться, что падают**

Run: `cargo test provider 2>&1 | tail -20`
Expected: ошибка компиляции — `build_url` и `fetch` не найдены.

- [ ] **Step 3: Реализация**

Вставить в `src/provider.rs` перед тестами:

```rust
fn build_url(prompt: &str, w: u32, h: u32, seed: u32) -> Result<Url> {
    let mut url = Url::parse(BASE).context("некорректный базовый URL")?;
    url.path_segments_mut()
        .map_err(|_| anyhow::anyhow!("базовый URL не поддерживает сегменты пути"))?
        .push("prompt")
        .push(prompt);
    url.query_pairs_mut()
        .append_pair("width", &w.to_string())
        .append_pair("height", &h.to_string())
        .append_pair("seed", &seed.to_string())
        .append_pair("nologo", "true");
    Ok(url)
}

fn get_bytes(client: &reqwest::blocking::Client, url: Url) -> Result<Vec<u8>> {
    let response = client.get(url).send().context("сеть недоступна")?;
    let status = response.status();
    if !status.is_success() {
        let body = response.text().unwrap_or_default();
        let head: String = body.chars().take(200).collect();
        bail!("сервис ответил {status}: {head}");
    }
    Ok(response.bytes().context("не удалось прочитать ответ")?.to_vec())
}

/// Запрашивает картинку. Пропорция соблюдается сервисом, абсолютный
/// размер — нет: ответ приходит с бюджетом около 0.59 Мп.
///
/// Прокси настраивать не нужно: reqwest с фичей system-proxy сам читает
/// HTTP_PROXY и HTTPS_PROXY из окружения.
pub fn fetch(prompt: &str, w: u32, h: u32, seed: u32) -> Result<DynamicImage> {
    let client = reqwest::blocking::Client::builder()
        .timeout(TIMEOUT)
        .build()
        .context("не удалось создать HTTP-клиент")?;

    let bytes = match get_bytes(&client, build_url(prompt, w, h, seed)?) {
        Ok(b) => b,
        Err(first) => {
            // Сервис периодически залипает. Повторяем с другим seed:
            // это заодно обходит возможный застрявший кеш.
            eprintln!("Первая попытка не удалась ({first:#}). Повторяю…");
            let retry_seed = seed.wrapping_add(1);
            get_bytes(&client, build_url(prompt, w, h, retry_seed)?)
                .with_context(|| format!("повтор тоже не удался (первая ошибка: {first:#})"))?
        }
    };

    image::load_from_memory(&bytes).context("ответ сервиса не является картинкой")
}
```

- [ ] **Step 4: Запустить тесты без сети**

Run: `cargo test provider`
Expected: PASS, 3 теста; сетевой помечен как ignored.

- [ ] **Step 5: Запустить сетевой тест вручную**

Run: `cargo test -- --ignored provider 2>&1 | tail -20`
Expected: PASS. Занимает до 40 секунд. Если сервис недоступен — это внешний сбой, не дефект кода; повторить.

- [ ] **Step 6: Коммит**

```bash
git add src/provider.rs src/main.rs
git commit -m "feat(provider): запрос картинки у Pollinations с повтором"
```

---

### Task 7: Сборка воедино

**Files:**
- Modify: `src/main.rs`

**Interfaces:**
- Consumes: всё из задач 1–6.
- Produces: работающий бинарник.

- [ ] **Step 1: Написать main**

Заменить `src/main.rs` целиком:

```rust
mod cli;
mod output;
mod prompt;
mod provider;
mod resize;
mod screen;

use anyhow::Result;
use clap::Parser;
use rand::prelude::*;

fn main() {
    if let Err(e) = run() {
        eprintln!("Ошибка: {e:#}");
        std::process::exit(1);
    }
}

fn run() -> Result<()> {
    let args = cli::Args::parse();
    let mut rng = rand::rng();

    let (width, height) = args.resolution.unwrap_or_else(screen::detect);
    let text = args.prompt.unwrap_or_else(|| prompt::random(&mut rng));
    // Одинаковый seed возвращает закешированную картинку, поэтому
    // без явного значения он обязан быть случайным.
    let seed = args.seed.unwrap_or_else(|| rng.random());
    let path = output::resolve_path(args.file, &output::timestamp());

    println!("Промпт: {text}");
    println!("Размер: {width}x{height}, seed: {seed}");
    println!("Генерация занимает около 40 секунд…");

    let img = provider::fetch(&text, width, height, seed)?;
    println!("Получено {}x{}, масштабирую…", img.width(), img.height());

    let scaled = resize::to(img, width, height);
    output::save(&scaled, &path)?;

    println!("Готово: {}", path.display());
    Ok(())
}
```

- [ ] **Step 2: Прогнать все тесты и линтер**

Run: `cargo test && cargo clippy -- -D warnings && cargo fmt --check`
Expected: все тесты PASS, clippy без предупреждений, форматирование чистое. Если `cargo fmt --check` ругается — выполнить `cargo fmt` и перепроверить.

- [ ] **Step 3: Ручная проверка по умолчанию**

```bash
cd /tmp && rm -f gpic-*.jpg
cargo run --manifest-path /home/user/mr/ws/gpic/Cargo.toml
```
Expected: печатается промпт, размер `1920x1200` и seed; появляется файл `gpic-ГГГГММДД-ЧЧММСС.jpg`. Проверить: `identify gpic-*.jpg` показывает `1920x1200`.

- [ ] **Step 4: Ручная проверка опций**

```bash
cd /tmp
cargo run --manifest-path /home/user/mr/ws/gpic/Cargo.toml -- -r 800x600 -p "ночной город в дожде" out.png
```
Expected: файл `/tmp/out.png`, `identify out.png` показывает `800x600` PNG.

- [ ] **Step 5: Проверка воспроизводимости по seed**

Взять seed из вывода шага 3 и выполнить с тем же промптом и `-s <seed>` — должна получиться та же картинка. Сравнить: `cmp первый.jpg второй.jpg` (побайтовое совпадение ожидаемо, поскольку сервис отдаёт закешированный результат).

- [ ] **Step 6: Проверка ошибок**

```bash
cd /tmp
cargo run --manifest-path /home/user/mr/ws/gpic/Cargo.toml -- -r 1920*1200
cargo run --manifest-path /home/user/mr/ws/gpic/Cargo.toml -- /nonexistent-dir-xyz/a.jpg
```
Expected: в обоих случаях внятное русское сообщение в stderr и код возврата 1 (`echo $?`), без паники и стектрейса.

- [ ] **Step 7: Обновить CLAUDE.md**

Заменить раздел «Состояние репозитория» на краткое описание архитектуры: назначение утилиты, шесть модулей и их роли, ключевой факт про пиксельный бюджет API 0.59 Мп и локальный ресайз, ссылку на спеку. Убрать фразу о том, что кода ещё нет.

- [ ] **Step 8: Коммит и пуш**

```bash
git add src/main.rs CLAUDE.md
git commit -m "feat: собрать gpic воедино"
git push origin main
```
